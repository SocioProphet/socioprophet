# Not Found View

## Purpose
Provides a 404 page for unknown routes.

## Key Files
- `NotFound.tsx` – Renders the header and a 404 message.
- `styles.tsx` – Styled-components for centered layout and responsive typography.

## Notes
- This view is mapped to `*` in `src/routes.js` to catch all unmatched routes.
