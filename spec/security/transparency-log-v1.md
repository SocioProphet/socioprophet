# Transparency Log v1 (Spec Stub)

Append-only log format for critical events (publish, deploy, key rotation, model release, policy adoption).

## Requirements
- content-addressed entries
- witness cosignatures (N-of-M with diversity constraints)
- inclusion proofs
- public query surfaces

## Witness policy
See: spec/security/witness-diversity-policy-v1.md
