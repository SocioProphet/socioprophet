# Contributing

We prefer:
- Small PRs
- Test-backed changes
- Clear commit messages
- Deterministic local verification

## Workflow

1. Create branch
2. Make focused changes
3. Run relevant tests
4. Open PR

If modifying Firestore rules:
cd socioprophet-web && npm ci && npm run test:rules
